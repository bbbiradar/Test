apiVersion: v1
kind: Service
metadata:
  name: {{ include "chart.yaml.fullname" . }}
  labels:
    {{- include "chart.yaml.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chart.yaml.selectorLabels" . | nindent 4 }}
